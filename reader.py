import csv
# Now it's your turn to write everything!
# For this problem, you need to read through a comma-separated document, and sum up
# the values in each column. THEN, you need to convert that list of values
# into a sentence using the key at the bottom! Each index corresponds to a letter.

key = tuple('abcdefghijklmnopqrstuvwxyz ')

with open("vals.csv") as fin:
	# Your code goes here!